<nav class="navbar navbar-default navbar-fixed-top" role="navigation">
  <ul class="container">
      <div class="banner">
        <h3>Mission San Jose Invitational Wrestling Tournament</h3>
        <h4>Feb 6-7, 2015</h4>
      </div>
      <li class="pull-left">
          <% if user_signed_in? %> Welcome <%= current_user.name %><br>
            <%= link_to "Logout", destroy_user_session_path, :method => :delete %>
          <% end %>
      </li>

      <li class="pull-right">
        <button type="button" class="btn btn-default"><%= link_to "Home",       root_path %></button>
        <% if user_signed_in? == false %>
          <%= link_to "Sign in", new_user_session_path %>
        <% end %>
        <% if user_signed_in? %>
          <% if current_user.admin? %>
            <div class="btn-group">
              <button type="button" class="btn btn-default dropdown-toggle" data-toggle="dropdown" aria-expanded="false">ADMIN<span class="caret"></span></button>
              <ul class="dropdown-menu" role="menu">
                <li><%= link_to "ADD USER", new_user_path %></li>
                <li><%= link_to "EDIT USERS", users_path %></li>
                <li><%= link_to "EDIT TEAMS", teams_path %></li>
                <li><%= link_to "WRESTLERS", wrestlers_path %></li>
              </ul>
            </div>

          <div class="btn-group">
            <button type="button" class="btn btn-default dropdown-toggle" data-toggle="dropdown" aria-expanded="false">ROSTERS<span class="caret"></span></button>
            <ul class="dropdown-menu" role="menu">
              <% @teams.each do |team| %>
                <div style="width: 300px;">
                  <li><%= link_to "#{team.name}", team_wrestlers_path(team) %></li>
                </div>
              <% end %>
            </ul>
          </div>
          <% end %>

          <% if !current_user.admin? %>
          <div class="btn-group">
            <button type="button" class="btn btn-default dropdown-toggle" data-toggle="dropdown" aria-expanded="false">
              TEAM INFO <span class="caret"></span>
            </button>
            <ul class="dropdown-menu" role="menu">
              <li><%= link_to "ENTER MY TEAM", users_home_path %></li>
              <li><%= link_to "VIEW OTHER TEAMS" %></li>
              <li class="divider"></li>
              <li><%= link_to "Logout", destroy_user_session_path, :method => :delete %></li>
            </ul>
          </div>
          <% end %>
        <% end %>
      </div>

  </ul>


</nav>





