 <nav class="navbar navbar-default navbar-fixed-top hidden-xs hiddem-sm" role="navigation">
  <div class="container">
    <div class="row">
      <div class="col-xs-2 vcenter">
        <div class="pull-left">
          <div style="height:3em">
          <% if user_signed_in? %> Welcome <%= current_user.name %><br>
            <%= link_to "Logout", destroy_user_session_path, :method => :delete %>
          <% end %>
        </div>
        </div>
      </div><!--

      --><div class="col-xs-6 vcenter">
        <div class="banner">
          <div class="vcenter-2">
          NCS / Les Schwab Tires Wrestling Championships
         <h3>Presented by Farmers</h3>
          Feb 27-28, 2015
        </div>
        </div>
      </div><!--

      --><div class="col-xs-4 vcenter">
        <div class="pull-right">
          <div style="height:3em">
          <%= link_to "Home ",       root_path %>
          <% if user_signed_in? == false %>/
            <%= link_to "Sign in", new_user_session_path %>
          <% end %>
          <% if user_signed_in? %>
            <% if current_user.admin? %>
              <div class="btn-group">
                <button type="button" class="btn btn-default dropdown-toggle" data-toggle="dropdown" aria-expanded="false">ADMIN<span class="caret"></span></button>
                <ul class="dropdown-menu" role="menu">
                  <li><%= link_to "ADD LEAGUE(user)", new_user_path %></li>
                  <li><%= link_to "VIEW LEAGUES / USERS", users_path %></li>
                  <li><%= link_to "VIEW ALL WRESTLERS", wrestlers_path %></li>
                </ul>
              </div>

              <div class="btn-group">
                <button type="button" class="btn btn-default dropdown-toggle" data-toggle="dropdown" aria-expanded="false">VIEW LEAGUE ENTRIES<span class="caret"></span></button>
                <ul class="dropdown-menu scrollable-menu" role="menu">
                  <% @users.each do |user| %>
                    <div style="width: 300px;">
                      <% if user.league != "" %>
                        <li><%= link_to "#{user.league} (#{user.wrestlers.count})", user_wrestlers_path(user) %></li>
                      <% end %>
                    </div>
                  <% end %>
                </ul>
              </div>
            <% end %>

            <% if !current_user.admin? %>
              <div class="btn-group">
                <button type="button" class="btn btn-default dropdown-toggle" data-toggle="dropdown" aria-expanded="false">
                  TOURNAMENT ENTRIES <span class="caret"></span>
                </button>
                <ul class="dropdown-menu scrollable-menu" role="menu">
                  <% if current_user.admin? || current_user.league != "" %>
                    <li><%= link_to "ENTER MY LEAGUE", user_wrestlers_path(current_user) %></li>
                  <% end %>
                  <li><%= link_to "VIEW OTHER LEAGUES", users_path %></li>
                  <li><%= link_to "VIEW ALL WRESTLERS", wrestlers_path %></li>
                  <li class="divider"></li>
                </ul>
              </div>
            <% end %>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</nav>

<nav class="navbar navbar-default navbar-fixed-top visible-xs visible-sm" role="navigation">
  <div class="container">
    <div class="row">
      <div class="col-xs-2 vcenter">
        <div class="pull-left">
          <div style="height:3em">
          <% if user_signed_in? %> Welcome <%= current_user.name %><br>
            <%= link_to "Logout", destroy_user_session_path, :method => :delete %>
          <% end %>
        </div>
        </div>
      </div><!--

      --><div class="col-xs-6 vcenter">
        <div class="banner">
          <div class="vcenter-2">
          NCS / Les Schwab Tires Wrestling Championships
        </div>
        </div>
      </div><!--

      --><div class="col-xs-4 vcenter">
        <div class="pull-right">
          <div style="height:3em">
          <%= link_to "Home ",       root_path %>
          <% if user_signed_in? == false %>/
            <%= link_to "Sign in", new_user_session_path %>
          <% end %>
          <% if user_signed_in? %>
            <% if current_user.admin? %>
              <div class="btn-group">
                <button type="button" class="btn btn-default dropdown-toggle" data-toggle="dropdown" aria-expanded="false">ADMIN<span class="caret"></span></button>
                <ul class="dropdown-menu" role="menu">
                  <li><%= link_to "ADD LEAGUE(user)", new_user_path %></li>
                  <li><%= link_to "VIEW LEAGUES / USERS", users_path %></li>
                  <li><%= link_to "VIEW ALL WRESTLERS", wrestlers_path %></li>
                </ul>
              </div>

              <div class="btn-group">
                <button type="button" class="btn btn-default dropdown-toggle" data-toggle="dropdown" aria-expanded="false">VIEW LEAGUE ENTRIES<span class="caret"></span></button>
                <ul class="dropdown-menu scrollable-menu" role="menu">
                  <% @users.each do |user| %>
                    <div style="width: 300px;">
                      <% if user.league != "" %>
                        <li><%= link_to "#{user.league} (#{user.wrestlers.count})", user_wrestlers_path(user) %></li>
                      <% end %>
                    </div>
                  <% end %>
                </ul>
              </div>
            <% end %>

            <% if !current_user.admin? %>
              <div class="btn-group">
                <button type="button" class="btn btn-default dropdown-toggle" data-toggle="dropdown" aria-expanded="false">
                  TOURNAMENT ENTRIES <span class="caret"></span>
                </button>
                <ul class="dropdown-menu scrollable-menu" role="menu">
                  <% if current_user.admin? || current_user.league != "" %>
                    <li><%= link_to "ENTER MY LEAGUE", user_wrestlers_path(current_user) %></li>
                  <% end %>
                  <li><%= link_to "VIEW OTHER LEAGUES", users_path %></li>
                  <li><%= link_to "VIEW ALL WRESTLERS", wrestlers_path %></li>
                  <li class="divider"></li>
                </ul>
              </div>
            <% end %>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</nav>




