<nav class="navbar navbar-default navbar-static-top" role="navigation">
  <div class="container">
    <h3>The 40th Mission San Jose Invitational</h3>
    <h4>Feb 6-7, 2015</h4>
    <div class="nav pull-left">
        <% if user_signed_in? %>
          Welcome <%= current_user.email %>
        <% end %>
    </div>
    <div class="nav pull-right">
      <button type="button" class="btn btn-default"><%= link_to "Home",       root_path %></button>
      <% if user_signed_in? == false %>
        <%= link_to "Sign in", new_user_session_path %>
      <% end %>
      <% if current_user.admin? %>
        <button type="button" class="btn btn-default"><%= link_to "EDIT TEAMS",       teams_path %></button>
      <% end %>
      <% if user_signed_in? %>
        <div class="btn-group">
          <button type="button" class="btn btn-default dropdown-toggle" data-toggle="dropdown" aria-expanded="false">
            TEAM INFO <span class="caret"></span>
          </button>
          <ul class="dropdown-menu" role="menu">
            <li><%= link_to "User Profile", edit_user_registration_path %></li>
            <li><%= link_to "Roster",      teams_path %></li>
            <li class="divider"></li>
            <li><%= link_to "Logout", destroy_user_session_path, :method => :delete %></li>
          </ul>
        </div>
      <% end %>
    </div>
  </div>
</nav>





